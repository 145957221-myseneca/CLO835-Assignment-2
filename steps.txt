sudo yum install -y yum-utils shadow-utils
sudo yum-config-manager --add-repo https://rpm.releases.hashicorp.com/AmazonLinux/hashicorp.repo
sudo yum -y install terraform

RUN pip3 install flask
Werkzeug==2.0.3

---------
docker push sql
docker pull uri

docker run --name new_db --net mynetwork1 -d -e MYSQL_ROOT_PASSWORD=password123 uri

docker push app
docker pull uri

export DBHOST=172.18.0.2
export DBPORT=3306
export DBUSER=root
export DATABASE=employees
export DBPWD=password123

sudo docker run -d --net mynetwork1 -p 8081:8080 \
-e DBHOST=$DBHOST -e DBPORT=$DBPORT \
-e DBUSER=$DBUSER -e DBPWD=$DBPWD \
-e APP_COLOR=blue --name blue \
168491602991.dkr.ecr.us-east-1.amazonaws.com/webapp:v1.1



docker exec -it blue /bin/bash